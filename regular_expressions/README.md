# Regular Expressions

This project covers the basics of regular expressions in Ruby.

## Files

- `0-simply_match_school.rb`: Ruby script that takes one argument and prints every match of `School` using a regular expression.

## Usage

Output: `SchoolSchool`
